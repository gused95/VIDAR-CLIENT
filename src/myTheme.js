import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#64dd17',
    },
    secondary: {
      main: '#d500f9',
    },
  },
});

export default theme;