import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    type: 'light',
    primary: {
      main: '#08250E',
    },
    secondary: {
      main: '#9BC437',
    },
  },
});

export default theme;